# Технології та інструменти Personal Assistant CLI

## Мова програмування

### Python 3.10+
- **Версія:** Python 3.10 або новіший
- **Чому обрано:** Сучасні можливості мови (pattern matching, улучшена типізація, dataclasses)
- **Особливості використання:**
  - Type hints для статичної типізації
  - Dataclasses для зменшення boilerplate коду
  - Context managers для роботи з файлами
  - List/Dict comprehensions для компактного коду

---

## Вбудовані бібліотеки Python

### 1. **dataclasses** (Python 3.7+)
```python
@dataclass
class AppContext:
    address_book: AddressBook
    notebook: Notebook
    storage: Storage
```
- Автоматична генерація `__init__`, `__repr__`, `__eq__`
- Зменшення boilerplate коду
- Чітка структура даних

### 2. **typing** (Type Hints)
```python
from typing import Dict, List, Optional, Callable, Tuple
```
- Статична типізація для кращої читабельності
- IDE підтримка автодоповнення
- Ранній виявлення помилок

### 3. **collections.UserDict**
```python
class AddressBook(UserDict[str, ContactRecord]):
```
- Базовий клас для створення власних словників
- Спрощене розширення функціональності dict
- Підтримка всіх стандартних методів словників

### 4. **re** (Regular Expressions)
```python
PHONE_PATTERN = re.compile(r"^\+?\d{7,15}$")
EMAIL_PATTERN = re.compile(r"^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$")
```
- Валідація телефонів та email
- Ефективний пошук та перевірка форматів
- Компіляція патернів для швидкості

### 5. **datetime**
```python
from datetime import datetime, timedelta
```
- Робота з датами народження
- Обчислення днів до дня народження
- Форматування дат у стандарті ISO (YYYY-MM-DD)

### 6. **json**
```python
import json
json.dump(data, file, indent=2, ensure_ascii=False)
```
- Серіалізація та десеріалізація даних
- Збереження контактів та нотаток у JSON
- Підтримка Unicode символів

### 7. **pathlib.Path**
```python
from pathlib import Path
storage_path = Path.home() / ".personal_assistant"
```
- Об'єктно-орієнтований підхід до роботи з файлами
- Кросплатформенність (Linux, macOS, Windows)
- Зручне створення та перевірка шляхів

### 8. **shlex**
```python
import shlex
tokens = shlex.split('add contact "John Doe" phone=+380501234567')
```
- Парсинг команд з підтримкою лапок
- Коректна обробка пробілів у значеннях
- Shell-like синтаксис для користувача

### 9. **difflib**
```python
from difflib import get_close_matches
suggestion = get_close_matches("ad contact", commands, n=1, cutoff=0.5)
```
- Пошук найближчих за написанням команд
- Інтелектуальні підказки при помилках
- Покращення user experience

### 10. **sys**
```python
import sys
sys.exit(main())
```
- Обробка exit codes
- Взаємодія з інтерпретатором

---

## Патерни проєктування

### 1. **Command Pattern**
```python
CommandFunc = Callable[[AppContext, str], CommandResult]
command_map: Dict[str, Tuple[CommandFunc, str]]
```
- Інкапсуляція команд як об'єктів
- Легке додавання нових команд
- Уніфікований інтерфейс

### 2. **Registry Pattern**
```python
class CommandRegistry:
    def resolve(self, user_input: str) -> tuple[str, CommandFunc, str, str]
```
- Централізоване управління командами
- Динамічна реєстрація хендлерів

### 3. **Descriptor Pattern**
```python
class Field:
    @property
    def value(self):
        return self._value
    
    @value.setter
    def value(self, value):
        self._value = self.validate(value)
```
- Валідація на рівні присвоєння
- Переповторне використання логіки валідації
- Інкапсуляція правил перевірки

### 4. **Repository Pattern**
```python
class AddressBook(UserDict):
    def add_record(self, record)
    def get(self, name)
    def search(self, query)
```
- Абстракція роботи з даними
- Відділення бізнес-логіки від зберігання

### 5. **Strategy Pattern**
- Різні стратегії пошуку (за ім'ям, тегом, вмістом)
- Валідація різних типів полів через спільний інтерфейс

### 6. **Dependency Injection**
```python
def __init__(self, storage: Storage | None = None):
    self.storage = storage or Storage()
```
- Тестованість коду
- Можливість підміни залежностей

---

## Архітектурні принципи

### SOLID Principles

#### 1. **Single Responsibility Principle (SRP)**
- `fields.py` — тільки валідація полів
- `storage.py` — тільки робота з файлами
- `commands.py` — тільки бізнес-логіка команд
- `cli.py` — тільки UI взаємодія

#### 2. **Open/Closed Principle (OCP)**
- Легко додавати нові команди без зміни існуючого коду
- Нові поля додаються через успадкування від `Field`

#### 3. **Dependency Inversion Principle (DIP)**
- Високорівневі модулі не залежать від низькорівневих
- Використання абстракцій (AppContext, Storage)

### Clean Code Practices

- **Осмислені назви:** `add_contact`, `upcoming_birthdays`, `search_by_tag`
- **Короткі функції:** кожна функція виконує одну задачу
- **Відсутність магічних чисел:** `days_ahead`, `cutoff=0.5`
- **Обробка помилок:** custom exceptions (`CommandError`, `ValidationError`)

### DRY (Don't Repeat Yourself)

- Переповторне використання `parse_key_value_args()`
- Спільний базовий клас `Field` для всіх типів полів
- Уніфікований `CommandResult` для всіх команд

---

## Стандарти коду

### PEP 8 — Style Guide for Python Code
- Назви класів: `CapWords` (AddressBook, ContactRecord)
- Назви функцій: `snake_case` (add_contact, days_to_birthday)
- Константи: `UPPER_CASE` (DEFAULT_STORAGE_DIR, DATE_FORMAT)
- Максимальна довжина рядка: 88 символів (Black formatter)
- Імпорти: групування (stdlib, third-party, local)

### PEP 257 — Docstring Conventions
```python
class AddressBook(UserDict):
    """Container for contact records with helper search utilities."""
```

### PEP 484 — Type Hints
```python
def search(self, query: str) -> List[ContactRecord]:
```

### PEP 585 — Type Hinting Generics (Python 3.9+)
```python
def resolve(self) -> tuple[str, CommandFunc, str, str]:
# замість Tuple[str, CommandFunc, str, str]
```

---

## Інструменти розробки

### Система збирання

#### setuptools + pyproject.toml
```toml
[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"
```
- Сучасний стандарт пакування Python (PEP 517, PEP 518)
- Декларативна конфігурація
- Entry points для CLI команд

### Система контролю версій

#### Git + GitHub
- Версіонування коду
- Історія змін
- Колаборація та code review
- `.gitignore` для виключення службових файлів

---

## Формати даних

### JSON (JavaScript Object Notation)
```json
{
  "name": "Alice",
  "phones": ["+380501234567"],
  "email": "alice@example.com",
  "birthday": "1990-03-15"
}
```
- **Переваги:**
  - Читабельний текстовий формат
  - Підтримка Unicode
  - Широка сумісність
  - Легкий backup та міграція
- **Використання:**
  - Збереження контактів (`contacts.json`)
  - Збереження нотаток (`notes.json`)

---

## Особливості реалізації

### 1. **REPL (Read-Eval-Print Loop)**
```python
while True:
    user_input = input("> ").strip()
    # parse and execute
```
- Інтерактивний режим роботи
- Immediate feedback
- Graceful shutdown (Ctrl+C, Ctrl+D)

### 2. **Case-insensitive пошук**
```python
self.data[record.name.value.lower()] = record
```
- Нормалізація ключів до нижнього регістру
- Зручність для користувача

### 3. **Автоматичне збереження**
- Персистентність після кожної операції
- Захист від втрати даних
- Відсутність команд "save"/"load"

### 4. **Unicode Support**
```python
with file.open("r", encoding="utf-8") as fh:
    json.load(fh)
```
- Підтримка кирилиці
- Міжнародні символи
- Emoji в нотатках ✅

### 5. **Валідація на рівні даних**
- Перевірка при присвоєнні значення
- Ранній виявлення помилок
- Консистентність даних

---

## Тестування (можливості)

### Інжекція залежностей для тестів
```python
# Production
cli = PersonalAssistantCLI()

# Testing
test_storage = Storage(base_dir=tmp_path)
cli = PersonalAssistantCLI(storage=test_storage)
```

### Фіксовані дати для тестування
```python
record.days_to_birthday(today=datetime(2024, 1, 1))
```

---

## Безпека та надійність

### 1. **Валідація вводу**
- Regex для телефонів та email
- Перевірка формату дат
- Захист від injection через `shlex`

### 2. **Обробка помилок**
```python
try:
    result = handler(context, arguments)
except CommandError as error:
    print(f"Error: {error}")
    continue  # Не падає, продовжує роботу
```

### 3. **Graceful degradation**
- Програма не падає при помилках
- Зрозумілі повідомлення про помилки
- Підказки при невідомих командах

---

## Кросплатформенність

### Підтримувані ОС
- ✅ **Linux** (будь-який дистрибутив)
- ✅ **macOS** (10.x+)
- ✅ **Windows** (10/11)

### Кросплатформенні рішення
- `pathlib.Path` замість `os.path`
- `Path.home()` для домашньої директорії
- UTF-8 кодування файлів
- Відсутність OS-специфічних команд

---

## Продуктивність

### Оптимізації

1. **Компіляція regex патернів**
```python
PHONE_PATTERN = re.compile(r"^\+?\d{7,15}$")
```
Компілюється один раз, використовується багато разів

2. **Використання множин для тегів**
```python
tags: Set[str] = field(default_factory=set)
```
O(1) пошук замість O(n) у списку

3. **Словники для індексації**
```python
UserDict[str, ContactRecord]  # O(1) доступ по імені
```

4. **Ледачі обчислення**
- Дані завантажуються лише при старті
- Збереження лише після змін

---

## Розширюваність

### Модульна архітектура
- Легко додати нові команди
- Можливість додати нові типи полів
- Розширення без зміни існуючого коду

### Майбутні можливості
- Заміна JSON на SQLite
- Додавання REST API
- Web інтерфейс
- Синхронізація з хмарою
- Експорт у різні формати (CSV, vCard)

---

## Висновок технологічного стеку

### Використано:
✅ **Python 3.10+** — сучасна мова програмування  
✅ **10+ вбудованих бібліотек** — без зовнішніх залежностей  
✅ **6 патернів проєктування** — професійна архітектура  
✅ **SOLID принципи** — якісний код  
✅ **PEP стандарти** — відповідність best practices  
✅ **Type hints** — статична типізація  
✅ **JSON** — простий і надійний формат зберігання  
✅ **Кросплатформенність** — працює скрізь  

### Переваги підходу:
- 🚀 Нуль зовнішніх залежностей (портативність)
- 📚 Читабельний та підтримуваний код
- 🛡️ Надійна валідація та обробка помилок
- 🔧 Легко розширювати та тестувати
- 🎯 Готовий до production використання

